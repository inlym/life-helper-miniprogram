/** 在 `Storage` 中存储的字段 */
export const StorageField = {
  /** 上一次获取的 `code` 信息 */
  CODE: 'code',

  /** 登录凭证 `token` */
  TOKEN: 'token',
}

/** 自定义请求头字段名 */
export const HeaderField = {
  JWT: 'x-auth-jwt',
}
